# Variáveis de ambiente — Blog Royal B&B
# Copie para .env (local) ou preencha no Railway (Variables).
# Substitua os valores abaixo pelas suas chaves reais.

# --- Scraping de artigos (por padrão: Trafilatura, gratuito) ---
# Opcional: usar RapidAPI em vez de Trafilatura
# Nome: USE_RAPIDAPI     |  Valor: 1 para ativar
# Nome: ARTICLE_SCRAPER_API_KEY  |  Sua chave RapidAPI (só se USE_RAPIDAPI=1)
# USE_RAPIDAPI=0
# ARTICLE_SCRAPER_API_KEY=sua_chave_rapidapi_aqui

# --- OpenRouter (LLM) ---
# Nome: OPENROUTER_API_KEY
OPENROUTER_API_KEY=sk-or-v1-sua_chave_aqui
# Nome (opcional): OPENROUTER_MODEL
OPENROUTER_MODEL=

# --- Fal (imagens: nano-banana-pro) ---
# Nome: FAL_KEY  (formato: id:secret)
FAL_KEY=seu_fal_id:seu_fal_secret
# Nome (opcional): FAL_GENERATE_INLINE  |  Valor: 0 = só capa via Fal, inline usa Unsplash (reduz custo). 1 = capa + inline via Fal.
# FAL_GENERATE_INLINE=0

# --- Banco de dados (opcional; recomendado no Railway) ---
# Se definido, usa PostgreSQL (ex.: Supabase). Se não definido, usa SQLite em data/blog_agua.db.
# Supabase: Project Settings → Database → Connection string (URI), ex.:
#   postgresql://postgres.[ref]:[SENHA]@aws-0-[região].pooler.supabase.com:6543/postgres
# Use a connection string "Transaction" (porta 5432) ou "Session" (6543) — ambas funcionam.
# Se a senha tiver @, #, % etc., codifique na URL: @ → %40, # → %23, % → %25.
# DATABASE_URL=postgresql://postgres.xxxx:senha@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# --- Push para GitHub (repo → Vercel redeploya o site) ---
# Onde configurar:
#   • No PC: arquivo .env na raiz do projeto (este arquivo é ignorado pelo git)
#   • No Railway: projeto → Variables → Add Variable
# Nome: PUSH_TO_GITHUB  |  Valor: 1
# Nome: GITHUB_TOKEN    |  Valor: GitHub Personal Access Token (com permissão repo)
# Nome: GITHUB_REPO     |  Valor: owner/repo (ex: Petriccone/blog-royal-bb) — obrigatório no Railway se o container não tiver .git
PUSH_TO_GITHUB=1
GITHUB_TOKEN=ghp_sua_chave_aqui
GITHUB_REPO=Petriccone/blog-royal-bb
