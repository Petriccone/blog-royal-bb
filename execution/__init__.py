"""
Pacote de scripts determinísticos da camada de Execução.

Cada módulo aqui deve expor funções claras (por exemplo, `run(params: dict) -> dict`)
que possam ser chamadas pelo agente com base nas diretivas em `directives/`.
"""

